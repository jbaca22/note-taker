// requires the router to be used by express and specifies a path
var path = require("path");
var router = require("express").Router();


//exports code to be calle by express in server.js
module.exports = router;